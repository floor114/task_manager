<h1>Tasks#index</h1>
- @tasks.each do |task|
  %p
    = "#{task.text}"
    = link_to 'Edit', edit_task_path(task)
    = link_to 'Delete', task_path(task), method: :delete, data: { confirm: 'Are you sure?' }
    = link_to 'Share', share_task_path(task), remote: true, method: :post
